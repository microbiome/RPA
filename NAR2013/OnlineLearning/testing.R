#batch.size <- 50
#cels <- sample(list.celfiles(cel.path, full.names = T))[1:100]
#res <- rpa.online(cel.files = cels, 
#     	 	   batch.size = batch.size,                 		 
#            	   save.batches.dir = temp.dir, keep.batch.files = FALSE, unique.run.identifier = uid)

